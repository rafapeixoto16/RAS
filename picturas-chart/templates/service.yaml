apiVersion: v1
kind: Service
metadata:
  name: {{ include "picturas-chart.fullname" . }}
  labels:
    {{- include "picturas-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "picturas-chart.selectorLabels" . | nindent 4 }}
